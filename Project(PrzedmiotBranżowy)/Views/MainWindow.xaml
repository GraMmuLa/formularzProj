<Window x:Class="Project_PrzedmiotBranzowy_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_PrzedmiotBranzowy_"
        xmlns:viewmodels="clr-namespace:Project_PrzedmiotBranzowy_.ViewModels"
        xmlns:views="clr-namespace:Project_PrzedmiotBranzowy_.Views"
        xmlns:prism="http://prismlibrary.com/"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="800">
    <ContentControl prism:RegionManager.RegionName="ContentRegion"/>
    <!--<ContentControl Content="{Binding CurrentViewModel}">
        <ContentControl.Resources>
            <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
                <views:HomeView/>
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>-->
</Window>
