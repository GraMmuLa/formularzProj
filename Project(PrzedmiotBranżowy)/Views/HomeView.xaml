<UserControl x:Class="Project_PrzedmiotBranżowy_.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_PrzedmiotBranżowy_.Views"
             xmlns:viewmodels="clr-namespace:Project_PrzedmiotBranżowy_.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="20px">
        <Label Content="Виберіть тест" HorizontalAlignment="Left"/>
        <ListBox Height="200" ItemsSource="{Binding Tests}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Command="{Binding NavigateAdminLoginCommand}" HorizontalAlignment="Left" Margin="0,10,0,0" Content="Увійти як адміністратор"/>
    </StackPanel>
</UserControl>
